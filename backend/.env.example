# Server Configuration
PORT=5175
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# PostgreSQL Configuration (Render)
# For production (Render): Replace with your Render PostgreSQL credentials
# For local: Update with your local PostgreSQL credentials
DATABASE_URL=postgresql://postgres:Pakobenson%4002@localhost:5432/remmogo
DB_HOST=localhost
DB_DATABASE=remmogo
DB_USER=postgres
DB_PASSWORD=Pakobenson%4002
DB_PORT=5432

# CORS Origins (comma-separated list for production)
# Add your Vercel production URL here
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,https://re-mmogo.vercel.app
